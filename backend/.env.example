# =====================================================
# PostgreSQL Database Configuration (Railway Production - Private Network)
# =====================================================
DB_HOST=postgres.railway.internal
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=example_password_here
DB_NAME=railway

# =====================================================
# PostgreSQL Database Configuration (Public Network - opcional, para conexión externa)
# =====================================================
# DB_HOST=switchyard.proxy.rlwy.net
# DB_PORT=32092
# DB_USER=postgres
# DB_PASSWORD=example_password_here
# DB_NAME=railway

# =====================================================
# Server Configuration
# =====================================================
PORT=4000
NODE_ENV=production

# =====================================================
# JWT Configuration
# =====================================================
JWT_SECRET=example_jwt_secret_here

# =====================================================
# Frontend URL (for CORS in production)
# =====================================================
# For single frontend (Railway fullstack):
# FRONTEND_URL=https://cafeteria.up.railway.app
#
# For multiple frontends (e.g., Netlify + Railway):
# Separate URLs with commas
FRONTEND_URL=https://cafeteria.up.railway.app,https://your-site.netlify.app
