# =====================================================
# PostgreSQL Database Configuration (Railway Production)
# =====================================================
DB_HOST=postgres.railway.internal
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_database_password
DB_NAME=railway

# =====================================================
# PostgreSQL Database Configuration (Local Development)
# Alternative: use PGHOST, PGUSER, etc. for local development
# =====================================================
# PGUSER=postgres
# PGHOST=localhost
# PGDATABASE=elsaborcolombiano
# PGPASSWORD=cafe
# PGPORT=5432

# =====================================================
# Server Configuration
# =====================================================
PORT=4000
NODE_ENV=development

# =====================================================
# JWT Configuration
# =====================================================
JWT_SECRET=your_jwt_secret_here

# =====================================================
# Frontend URL (for CORS in production)
# Replace with your actual Railway URL
# =====================================================
# FRONTEND_URL=https://<tu-proyecto>.up.railway.app
