{
  "name": "cafeteria",
  "version": "1.0.0",
  "description": "![Version](https://img.shields.io/badge/version-1.0.0-blue) ![License](https://img.shields.io/badge/license-MIT-green)",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "build:railway": "BUILD_TARGET=railway-fullstack npm run build --prefix frontend",
    "build:netlify": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "seed": "npm run check-seed --prefix backend",
    "deploy": "npm run init-db --prefix backend && npm run start --prefix backend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jbarahona31/CAFETERIA.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/jbarahona31/CAFETERIA/issues"
  },
  "homepage": "https://github.com/jbarahona31/CAFETERIA#readme",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "pg": "^8.16.3"
  }
}
